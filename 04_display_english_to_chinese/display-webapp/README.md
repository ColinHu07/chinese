# Display Web App

Open `index.html` in a desktop browser for local layout testing.

For Ray-Ban Display testing, deploy this folder to a public HTTPS host and set `CAPTION_WS_URL` in `config.js` to a public `wss://` caption server.

This Web App is display-only. It does not capture microphone audio.
